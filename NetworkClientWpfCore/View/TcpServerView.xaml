<UserControl x:Class="NetworkClientWpfCore.View.TcpServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkClientWpfCore.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border CornerRadius="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,2">
                    <GradientStop Color="#FFFFDB78" Offset="0.0"/>
                    <GradientStop Color="#FFFCEFB3" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <StackPanel>
            <TextBlock Text="TCP Server"
               Foreground="White"
               Margin="10 10 0 0"
               FontSize="22">

            </TextBlock>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock FontSize="22" Text="IP:Port" VerticalAlignment="Center"/>

                <StackPanel Grid.Row="2" Grid.Column="2"  Orientation="Horizontal">
                    <TextBox Margin="5" HorizontalAlignment="Left" Text="{Binding Bind, UpdateSourceTrigger=PropertyChanged}" MinWidth="50"/>
                    <Button Content="Listen" Margin="5" Command="{Binding BindCommand}"/>
                </StackPanel>

            </Grid>

            <ListView ItemsSource="{Binding ReceivedMessages}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Peer">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Center" Text="{Binding Peer}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Message">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 20 0" Text="{Binding Message}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
