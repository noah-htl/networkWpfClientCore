<UserControl x:Class="NetworkClientWpfCore.View.TcpClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkClientWpfCore.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border CornerRadius="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,2">
                    <GradientStop Color="#FFFFC62B" Offset="0.0"/>
                    <GradientStop Color="#FFFFD152" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <StackPanel>
            <TextBlock Text="TCP Client"
               Foreground="White"
               Margin="10 10 0 0"
               FontSize="22">

            </TextBlock>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock FontSize="22" Text="IP-Adress/Portnummer:" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBox Margin="5" Text="{Binding ReceiverAddress, UpdateSourceTrigger=PropertyChanged}" MinWidth="100"/>
                    <Button Margin="5" Content="Connect" Command="{Binding ConnectCommand}"></Button>
                </StackPanel>
               

                <TextBlock Grid.Row="2" FontSize="22" Text="Message:" VerticalAlignment="Center"/>

                <StackPanel Grid.Row="2" Grid.Column="2"  Orientation="Horizontal">
                    <TextBox Margin="5" Width="200" HorizontalAlignment="Left" Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Send Message" Margin="5" Command="{Binding SendMessageCommand}"/>
                    <StackPanel Width="100"/>
                </StackPanel>

            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
